import config.Libs

plugins {
    id("LibraryModule")
    id("ArchModule")
}
apply from: rootProject.file("build_scripts/publish_lifecycle.gradle")

dependencies {
    api project(":kmvp")

    implementation Libs.AndroidX.core
    implementation Libs.AndroidX.appcompat
}