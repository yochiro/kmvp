import config.Libs

plugins {
    id("LibraryModule")
    id("DaggerModule")
}
apply from: rootProject.file("build_scripts/publish_dagger.gradle")

dependencies {
    api project(":kmvp")

    implementation Libs.AndroidX.core
    implementation Libs.AndroidX.appcompat
}