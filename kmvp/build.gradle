import config.Libs

plugins {
    id("LibraryModule")
}
apply from: rootProject.file("build_scripts/publish_core.gradle")

dependencies {
    implementation Libs.AndroidX.core
    implementation Libs.AndroidX.appcompat
}
